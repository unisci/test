use system 
sudo ifconfig
use ieee.logic
set url =url0
check dns
if dns =dns0
then use ip to the web_url_server
elsif dns &dns0 =0 then
use localhost_dns goto ip for server
end;
setup elstd;