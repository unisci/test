>>>for Node.js test
/*事件循环*/
while (queue.wait()) {
  queue.process();
};
if test ans = error , then 
start download at https://nodejs.org/en/download/;
finish;
start set up ;
end process;
end;
start test now 
node --version;
v[major version].[minor version].[patch version];
end fudn;
open ndjstesta.js;